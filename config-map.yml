apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  labels:
    app: k8s-app
    tier: k8s-web
data:
  DEBUG: "False"
  DATABASE_URL: "postgres://name:password@url"
  ALLOWED_HOSTS: "minikube ip"